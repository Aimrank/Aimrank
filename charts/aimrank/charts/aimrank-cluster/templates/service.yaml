apiVersion: v1
kind: Service
metadata:
  name: {{ include "aimrank-cluster.fullname" . }}
spec:
  selector: {{ include "aimrank-cluster.selectorLabels" . | nindent 4 }}
  ports:
  - protocol: TCP
    port: 80
